Milestone 2: integrate Cyanite and Songstats as LOAD-BEARING data sources, extending the existing /api/analyze pipeline and the Zod contract. Keep all four views and the live pipeline working. All calls server-side from Replit Secrets. Where exact API shapes are uncertain, READ THE PARTNER DOCS (Cyanite: api-docs.cyanite.ai; Songstats: their developer docs) for precise field/endpoint names — do not guess.

CONTRACT EXTENSIONS (shared types):
- ArcPoint stays {t,valence,intensity,emotion}.
- Song: add emotionSource:'cyanite'|'lyric', cyaniteSummary?:{moodTags:string[],valence:number,arousal:number,energy:string}, marketDataSource:'songstats'|'estimated', partnersUsed:string[].
- Market already has readiness/momentum/streamsDelta; add streamsHistory?:number[] (for a sparkline) and absoluteStreams?:number.

CYANITE (the source-of-truth emotional arc — this REPLACES the LLM-derived sourceArc):
- GraphQL at https://api.cyanite.ai/graphql, header "Authorization: Bearer <CYANITE_ACCESS_TOKEN>".
- Flow for a track's iTunes preview audio URL: create a LibraryTrack from the audio (use Cyanite's file-upload mutation flow — request an upload URL, PUT the audio bytes, create the libraryTrack with the upload id; confirm exact mutation names in docs). Then POLL libraryTrack(id){ audioAnalysisV7 { __typename ... on AudioAnalysisV7Finished { result { valence arousal energyLevel emotionalProfile moodTags segments { timestamps valence arousal } } } ... on AudioAnalysisV7Failed { error{message} } } } until Finished/Failed/timeout.
- Build fingerprint.sourceArc from result.segments: map timestamps→t (normalized to the song timeline), valence→valence (0..1), arousal→intensity, and bucket (valence,arousal) into one emotion of joy|heat|love|calm|melancholy. Save cyaniteSummary (top moodTags, valence, arousal, energy). Set emotionSource='cyanite'. Emotion Fidelity = the measured divergence between this Cyanite source arc and the translation arc.
- ASYNC HANDLING: Cyanite is slow. In the synchronous /api/analyze, do NOT block — return immediately with emotionSource='lyric' (the existing LLM arc) as a placeholder. Add a separate endpoint POST /api/enrich/cyanite {trackId, previewUrl} that runs+polls the Cyanite job and returns the cyanite arc + summary. The frontend calls it right after the initial render and swaps the arc in when ready. If Cyanite returns Failed/NotAuthorized or times out, keep emotionSource='lyric' silently.

SONGSTATS (the real market intelligence powering the cockpit):
- REST, server-side, key from SONGSTATS_API_KEY (confirm base URL, auth header, and endpoints in docs). Using the Musixmatch track ISRC (and/or artist), fetch track stats (streams, playlists, shazams, tiktok, historical) and artist insights (top markets by streams/audience, growth, platform breakdown).
- Build Market[] from REAL data: take the top ~6 markets; momentum and streamsDelta come from Songstats growth, streamsHistory from the historical series, absoluteStreams from current streams. Readiness stays derived from our per-language fidelity (the localization quality for that market's language). "Localize first" = the highest-momentum market with low readiness — now driven by real Songstats numbers. Set marketDataSource='songstats'. If no ISRC match or the call fails, fall back to estimated markets (marketDataSource='estimated').
- Songstats is reasonably fast: fetch it INLINE in /api/analyze with a timeout + the estimated fallback.

PRE-COMPUTE (featured songs load instantly with all real data): add a server script/route POST /api/precompute that, for the featured song list, runs the FULL pipeline INCLUDING waiting for Cyanite to finish + Songstats, and persists the complete derived Song (cyanite arc + summary + real markets) to the featured store. Featured analyses serve from this store — no waiting, no async at view time.

FRONTEND — make both integrations VISIBLE and WOW:
- CAST (Meaning-Cast), the dual emotional arc: render the CYANITE arc as a filled gradient AREA in the source emotion accent (what the MUSIC feels), beneath the translation-emotion curve (what the WORDS say). Where they diverge, make the gap GLOW in the accent — that glowing gap IS Emotion Fidelity, visualized. Draw the area on with animation. Label in Space Mono: "CYANITE · AUDIO EMOTION" and "LYRIC · TRANSLATION EMOTION". While emotionSource='lyric', show a subtle "ANALYZING AUDIO · CYANITE…" that upgrades to the real arc when /api/enrich/cyanite returns.
- WORLD (cockpit), a live intelligence terminal: nodes sized + pulsing by REAL Songstats momentum; each label shows real numbers (e.g., "BRAZIL · ▲34% · 1.2M/30D") with a tiny streamsHistory SPARKLINE beside it; values tick in on load; the opportunity callout is driven by real momentum. Add a faint equirectangular world-map backdrop so "global" is unmistakable. Label "SONGSTATS · LIVE MARKET DATA". Show "ESTIMATED" when marketDataSource='estimated'.
- A subtle provenance line (Space Mono) listing partnersUsed: "MUSIXMATCH · CYANITE · SONGSTATS".

EDGE CASES: Cyanite async never blocks the live path; Cyanite Failed/NotAuthorized/timeout → silent lyric-arc fallback; Songstats no-ISRC/failure → estimated markets; rate limits; featured fully pre-computed; contract unchanged for the views' rendering; TypeScript compiles. Show me a live analysis where the Cyanite audio arc swaps in under the translation arc with the divergence glowing, and the cockpit shows real Songstats momentum.